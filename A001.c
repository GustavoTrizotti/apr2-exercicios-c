#include <stdio.h>
// Faça um programa que leia dois números inteiros e imprima a soma deles.
int main() {
    int x, y;
    printf("\nDigite dois numeros inteiros: ");
    scanf("%d %d", &x, &y);
    printf("Soma: %d", x + y);
    return 0;
}